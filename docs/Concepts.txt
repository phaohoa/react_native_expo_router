===================================================
Phase 1.3: Dynamic Routes & Parameters
Key Concepts Summary:
===================================================

1. Dynamic Route Syntax:

[id].tsx → Matches any single segment
[category]/[id].tsx → Multiple dynamic segments
File name in brackets becomes the parameter name

2. Accessing Parameters:
const { id, category } = useLocalSearchParams();

3. Passing Parameters:
Method 1: String URL
<Link href="/users/123" />

Method 2: Object with pathname
<Link href={{
  pathname: '/users/[id]',
  params: { id: '123', name: 'John' }
}} />

Method 3: Programmatic
router.push({
  pathname: '/users/[id]',
  params: { id: '123' }
});


===================================================
Phase 2: Navigation Patterns
Key Concepts Summary:
===================================================
1. Stack Navigation:

Default navigation pattern
Screens stack on top of each other
Customizable headers with options
Per-screen or global configuration

2. Tab Navigation:

Bottom tabs for main app sections
Group routes with (tabs) folder
Icons, badges, and styling
Each tab has its own navigation stack

===================================================
Phase 3: Advanced Layouts & Protected Routes
Layout groups
Nested layouts
Protected Routes 🔒
Authentication Guards 🔐
Route protection patterns
===================================================
## 🎯 **Your Complete Structure Now:**
```
my-expo-router-app/
├── app/
│   ├── _layout.tsx                  # Root with AuthProvider
│   ├── index.tsx                    # Redirects to mode-selector
│   ├── mode-selector.tsx            # Choose Demo or Auth mode
│   │
│   ├── contexts/
│   │   └── AuthContext.tsx          # Auth logic
│   │
│   ├── (demo)/                      # 📚 Demo Mode - All old examples
│   │   ├── _layout.tsx
│   │   ├── home.tsx
│   │   ├── about.tsx
│   │   ├── profile.tsx
│   │   ├── settings.tsx
│   │   ├── navigation-demo.tsx
│   │   ├── custom-header.tsx
│   │   ├── modal-demo.tsx
│   │   ├── users/
│   │   ├── products/
│   │   ├── post/
│   │   ├── (tabs)/
│   │   └── ...
│   │
│   ├── (auth)/                      # 🔐 Auth Mode - Sign in/up
│   │   ├── _layout.tsx
│   │   ├── sign-in.tsx
│   │   └── sign-up.tsx
│   │
│   └── (protected)/                 # 🔒 Protected Routes
│       ├── _layout.tsx
│       └── home.tsx